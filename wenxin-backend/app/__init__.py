# This file intentionally left empty.
# It makes ``app`` a proper Python package so that ``pip install -e .``
# and ``from app.xxx import ...`` work correctly.
# Do NOT add imports here — circular-import risk with FastAPI/SQLAlchemy.
