/**
 * VULCA View Types
 * Separate file for view-related types to avoid import conflicts
 */

export type ViewMode = '6d' | '47d';
export type VisualizationType = 'radar' | 'heatmap' | 'bar' | 'parallel';